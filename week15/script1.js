// using any simple code:
function add(x, y) {
    console.log(x + y);
}

// in Node JS => module => a JS file!

// 
function getData() {
    // get data from a database
    // database? MySQL?
    // NoSQL database => MongoDB
}

// using express to send these data to the front-end side